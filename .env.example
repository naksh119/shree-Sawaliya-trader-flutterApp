# Copy to `.env` and adjust for your environment.
# Do not commit `.env` — it is gitignored.

# Django API base URL (use http://10.0.2.2:8000 for local Android emulator)
API_BASE_URL=https://api.sawaliyamultitrade.com

# Optional: third-party keys (uncomment when needed)
# SENTRY_DSN=
# GOOGLE_MAPS_API_KEY=
